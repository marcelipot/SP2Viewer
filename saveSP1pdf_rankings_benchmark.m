
if handles.existRankings == 1;
    if isempty(handles.databaseCurrent_rankings) == 0;
        if isempty(handles.databaseCurrentName_rankings) == 0;
            databaseCurrent = handles.databaseCurrent_rankings;
            nli = length(databaseCurrent(:,1));
            for i = 1:length(handles.databaseCurrentName_rankings(1,:));
                databaseCurrent{nli+1,i} = handles.databaseCurrentName_rankings{1,i};
            end;
        else;
            databaseCurrent = handles.databaseCurrent_rankings;
        end;
    end;
    handles.databaseCurrent = databaseCurrent;

    answerReport = 'SP1';

    RaceDist = str2num(databaseCurrent{1,3});
    Course = str2num(databaseCurrent{1,10});
    Gender = databaseCurrent{1,5};
    StrokeType = databaseCurrent{1,4};

    if strcmpi(lower(StrokeType), 'freestyle');
        StrokeShort = 'FS';
    elseif strcmpi(lower(StrokeType), 'medley');
        StrokeShort = 'IM';
    elseif strcmpi(lower(StrokeType), 'backstroke');
        StrokeShort = 'BK';
    elseif strcmpi(lower(StrokeType), 'breaststroke');
        StrokeShort = 'BR';
    elseif strcmpi(lower(StrokeType), 'butterfly');
        StrokeShort = 'BF';
    end;

    handles2.sourceReport = 'Comp';
    source = 'display';
    if Course == 25;
        if RaceDist == 50;
            create_SP1reportComp_SCM50_rankings;
        elseif RaceDist == 100;
            create_SP1reportComp_SCM100_rankings;
        elseif RaceDist == 150;
            create_SP1reportComp_SCM150_rankings;
        elseif RaceDist == 200;
            create_SP1reportComp_SCM200_rankings;
        elseif RaceDist == 400;
            create_SP1reportComp_SCM400_rankings;
        elseif RaceDist == 800;
            create_SP1reportComp_SCM800_rankings;
        elseif RaceDist == 1500;
            create_SP1reportComp_SCM1500_rankings;
        end;
        if strcmpi(lower(Gender), 'female') == 1;
            if strcmpi(answerReport, 'SP1') == 1;
                handles2.filename = ['Women_' num2str(RaceDist) StrokeShort '_SCM_SP1Comp'];
            else;
                handles2.filename = ['Women_' num2str(RaceDist) StrokeShort '_SCM_SP2Comp'];
            end;
        else;
            if strcmpi(answerReport, 'SP1') == 1;
                handles2.filename = ['Men_' num2str(RaceDist) StrokeShort '_SCM_SP1Comp'];
            else;
                handles2.filename = ['Men_' num2str(RaceDist) StrokeShort '_SCM_SP2Comp'];
            end;
        end;
    else;
        if RaceDist == 50;
            create_SP1reportComp_LCM50_rankings;
        elseif RaceDist == 100;
            create_SP1reportComp_LCM100_rankings;
        elseif RaceDist == 150;
            create_SP1reportComp_LCM150_rankings;
        elseif RaceDist == 200;
            create_SP1reportComp_LCM200_rankings;
        elseif RaceDist == 400;
            create_SP1reportComp_LCM400_rankings;
        elseif RaceDist == 800;
            create_SP1reportComp_LCM800_rankings;
        elseif RaceDist == 1500;
            create_SP1reportComp_LCM1500_rankings;
        end;
        if strcmpi(lower(Gender), 'female') == 1;
            handles2.filename = ['Women_' num2str(RaceDist) StrokeShort '_LCM_SP1Comp'];
        else;
            handles2.filename = ['Men_' num2str(RaceDist) StrokeShort '_LCM_SP1Comp'];
        end;
    end;
%     guidata(handles2.hdef, handles2);


else;
    return;
end;
% 
% set(Fig, 'units', 'normalized');
% Fig.Visible = 'off';
% Fig.InvertHardcopy = 'off';
% if ispc == 1;
%     if isfile([pathname '\' filename]) == 1;
%         MDIR = getenv('USERPROFILE');
%         command = ['del /Q /S ' pathname '\' filename];
%         [status, cmdout] = system(command);
%     end;
%     saveas(Fig, [pathname '\' filename]);
% elseif ismac == 1;
%     if isfile([pathname '/' filename]) == 1;
%         command = ['rm -rf ' pathname '/' filename];
%         [status, cmdout] = system(command);
%     end;
%     saveas(Fig, [pathname '/' filename]);
% end;
% clear_figures;
